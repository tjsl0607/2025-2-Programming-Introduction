/*
#include <stdio.h>
// 표준입출력 불러오기
#define _CRT_SECURE_NO_WARNINGS
//scanf 경고 방지

int main(void) {
	double light_speed = 300000.0;
	//빝의 속도 저장하는 변수 선언 및 초기화
	double distance = 14960 * 10000;
	//태양과 지구 사이 거리 저장하는 변수 선언 및 초기화
	double time;
	//시간 변수 선언

	time = distance/light_speed;
	//시간 계산 = 거리/ 속도

	printf("빛의 속도는 %.6lfkm/s\n", light_speed);
	//\n 해서 다음 줄로 잘 넘어감., 빛의 속도 출력
	printf("태양과 지구와의 거리는 %.6lfkm\n", distance);
	//lf를 사용하면 double 출력가능, 거리 출력
	printf("도달 시간은 %.6lf초\n", time);
	//계산한 시간 출력.

	printf("2510720 문예선");
	return 0;

}*/